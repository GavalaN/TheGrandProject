<Window x:Class="AAAdminApp.AddType"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Type" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Brand Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Brand:*" Margin="0,0,0,2"/>
            <ComboBox x:Name="cmbBrands" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      IsSynchronizedWithCurrentItem="True"/>
        </StackPanel>

        <!-- Type Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Type Name:*" Margin="0,0,0,2"/>
            <TextBox x:Name="txtType" Text="" 
                     MaxLength="50"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Mentés" Width="80" Margin="0,0,10,0" 
                     IsDefault="True" Click="Button_Click"/>
            <Button Content="Vissza" Width="80" 
                     IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>